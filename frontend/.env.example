# SkyTrack Frontend – Environment Variables
# Copy this file to .env and adjust as needed

# Backend API base URL (used with Vite proxy – keep as /api for dev)
VITE_API_URL=/api

# Socket.IO server URL (direct connection; CORS is enabled on the backend)
VITE_SOCKET_URL=http://localhost:3000
